//这里是一个简单的转换器，可以将BMS文件包里的wav格式的key音转换为ogg格式。
//由于作者的重度懒癌，这个转换器并没有制作完成。可能在未来的某一天完成，也可能永远也不会完成。

#include <stdio.h>
int main()
{
printf('BMSConverter v0.0 by Yuouzz\nUnder construction now\nIt is will be a program like wav2ogg');
return 0;
}
